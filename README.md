# Web App Prototype Design

This is a code bundle for Web App Prototype Design. The original design was created using a professional design tool and has been converted to a web application.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
